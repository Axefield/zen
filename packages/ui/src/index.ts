export type {
  ComponentStatus,
  TokenCategory,
  DesignToken,
  Typography,
  Color,
  Icon,
  Layout,
  ComponentSpec,
} from "@truligon/types"

export type { BaseEntity } from "@truligon/types"

export const TOKENS_CSS_PATH = "@truligon/ui/src/tokens.css"
