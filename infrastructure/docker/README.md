# Docker

Docker and Compose infrastructure for local development and production orchestration.

| File | Purpose |
|------|---------|
| `docker-compose.yml` | Local development (auto-applied) |
| `../docker-compose.prod.yml` | Production deployment (use with `-f ../docker-compose.prod.yml`) |
| `production.md` | Full production deployment guide |

See [production.md](./production.md) for deployment instructions.
