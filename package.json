{
  "name": "truligon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: not configured yet'",
    "test": "echo 'test: not configured yet'"
  },
  "overrides": {
    "esbuild": "0.25.12"
  }
}
